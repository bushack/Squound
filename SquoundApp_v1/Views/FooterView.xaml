<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:SquoundApp_v1.ViewModels"
             x:Class="SquoundApp_v1.Views.FooterView"
             x:DataType="viewmodels:FooterViewModel">

    <StackLayout Background="{StaticResource SquoundGradientBrush}">

        <BoxView HeightRequest="10"
                 BackgroundColor="Transparent"/>
        
        <!--<Button Text="About us"
                HorizontalOptions="Start"
                Style="{StaticResource TextButton}"
                Command="{Binding GoToPageCommand}"/>-->

        <Border BackgroundColor="Transparent"
                StrokeThickness="0">
            <Border.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding GoToAboutUsPageCommand}"/>
            </Border.GestureRecognizers>
            <Label Text="About us"
                   Style="{StaticResource FooterTextButton}"
                   SemanticProperties.Description="Learn more about us"/>
        </Border>

        <Button Text="Sell to us"
                HorizontalOptions="Start"
                Style="{StaticResource TextButton}"/>

        <Button Text="Something else"
                HorizontalOptions="Start"
                Style="{StaticResource TextButton}"/>

        <BoxView HeightRequest="10"
                 BackgroundColor="Transparent"/>
        
        <!--<Label Text="Follow us or get in touch"
               Style="{StaticResource FooterText}"
               SemanticProperties.Description="Follow us on social media or get in touch"
               HorizontalOptions="Fill"/>

        <BoxView HeightRequest="15"
                 BackgroundColor="Transparent"/>-->

        <FlexLayout HeightRequest="30"
                    HorizontalOptions="Fill"
                    VerticalOptions="Center"
                    JustifyContent="SpaceEvenly">

            <ImageButton Source="facebook.png" Style="{StaticResource FooterImageButton}"/>
            <ImageButton Source="instagram.png" Style="{StaticResource FooterImageButton}"/>
            <ImageButton Source="call.png" Style="{StaticResource FooterImageButton}"/>
            <ImageButton Source="email.png" Style="{StaticResource FooterImageButton}"/>
            <ImageButton Source="googlemaps.png" Style="{StaticResource FooterImageButton}"/>

        </FlexLayout>

        <BoxView HeightRequest="25"
                 BackgroundColor="Transparent"/>

        <Image Source="squound.png"
               HorizontalOptions="Center"
               WidthRequest="80"
               HeightRequest="80"/>

        <BoxView HeightRequest="10"
                 BackgroundColor="Transparent"/>

        <Label Text="&#169; 2025 Squound LLC. All Rights Reserved."
               Style="{StaticResource Footer}"
               SemanticProperties.Description="Follow us on social media or get in touch"
               HorizontalOptions="Fill"
               Padding="10"/>

    </StackLayout>

</ContentView>
