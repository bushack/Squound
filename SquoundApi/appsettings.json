{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Setup ASP.NET Web API to use SQL Server for project development.
  // Allows ASP.NET API and SQL Database to be hosted on PC and accessed by Windows Local Machine and/or a physical Android device.
  "ConnectionStrings": { "DefaultConnection": "Server=LENOVOZ570\\SQLEXPRESS;Database=Squound;Trusted_Connection=True;" }
}
